# PH WiFi Portal

**Enterprise WiFi Ad Monetization System for the Philippines Market.**

Turn free WiFi into recurring revenue — users watch a 30-second ad, then get 1 hour of internet access. Zero cost to users, passive income for you.

[繁體中文版在下方 ↓](#繁體中文)

---

## Architecture

```
                         ┌─────────────────────────┐
                         │   OC200 (Hardware Ctrl)  │
                         │   192.168.0.239          │
                         └────────────┬────────────┘
                                      │ Omada SDK API
         ┌──────────────┐             │
         │  EAP225      │─────────────┤
         │  (Indoor AP) │             │
         └──────────────┘             │
                         ┌────────────▼────────────┐
         ┌──────────────┐│                          │
         │ EAP650-Out   ││   [User connects WiFi]   │
         │ (Outdoor AP) │└────────────┬────────────┘
         └──────────────┘             │ HTTP redirect
                                      │
                         ┌────────────▼────────────┐
                         │   Portal Frontend        │
                         │   (Adcash 30s Ad)        │
                         └────────────┬────────────┘
                                      │ POST /api/grant-access
                         ┌────────────▼────────────┐
                         │   FastAPI Backend         │
                         │   + PostgreSQL + Redis   │
                         └────────────┬────────────┘
                                      │ Authorize client
                         ┌────────────▼────────────┐
                         │   OC200 Hotspot API      │
                         │   (grant internet access)│
                         └─────────────────────────┘
```

**Traffic Flow:**
1. User connects to `FreeWiFi_PH` (open SSID, no password)
2. OC200 redirects all HTTP traffic to `https://your-domain/portal`
3. Portal shows 30-second Adcash interstitial ad
4. After countdown, user clicks "Connect"
5. FastAPI calls OC200 API to authorize the user's MAC address
6. User gets 1 hour of internet access

---

## Quick Start

### Prerequisites

- Ubuntu 22.04 LTS VPS (1 vCPU / 1GB RAM minimum)
- Domain name pointed to your server IP
- TP-Link OC200 + EAP access points on the same network
- Adcash publisher account (see [docs/ADCASH-SETUP.md](docs/ADCASH-SETUP.md))

### Installation

```bash
# Clone the repository
git clone https://github.com/yourusername/ph-wifi-system.git
cd ph-wifi-system

# Run the one-click setup (Ubuntu 22.04)
sudo bash deploy/setup.sh
```

The setup script will:
- Install Docker & Docker Compose v2
- Prompt for all required configuration
- Generate `server/.env` with secure random keys
- Obtain Let's Encrypt SSL certificate automatically
- Start all services (Nginx, FastAPI, PostgreSQL, Redis)
- Run database migrations

### Configuration

All configuration is in `server/.env` (auto-generated by setup.sh):

```env
# Application
DOMAIN=portal.example.com
SECRET_KEY=<auto-generated 64-char hex>

# Omada Controller
OMADA_HOST=192.168.0.239
OMADA_PORT=8043
OMADA_CONTROLLER_ID=<from OC200 URL>
OMADA_OPERATOR=ph_wifi_operator
OMADA_PASSWORD=<operator password>

# Adcash
ADCASH_ZONE_KEY=<your zone key>

# Database (auto-generated)
POSTGRES_USER=ph_wifi_user
POSTGRES_PASSWORD=<auto-generated>
```

---

## Hardware

| Device | Model | Purpose | Notes |
|--------|-------|---------|-------|
| Controller | TP-Link OC200 | Manages all APs | Hardware controller, no cloud required |
| Indoor AP | TP-Link EAP225 | Indoor coverage | AC1200 Wi-Fi 5, ~30m radius, PoE |
| Outdoor AP | TP-Link EAP650-Outdoor | Outdoor coverage | AX3000 Wi-Fi 6, IP67 waterproof, ~50m |

**Recommended deployment:**
- 1× OC200 (connect to router LAN port)
- 2–4× EAP225 (café interior, lobby, waiting areas)
- 1–2× EAP650-Outdoor (parking lot, entrance, outdoor terrace)

All EAPs connect to OC200 via PoE switch.

---

## Deployment

### Option A: Zeabur (Recommended for Beginners)

Zeabur handles Docker orchestration, SSL, and domain routing automatically.

[![Deploy on Zeabur](https://zeabur.com/button.svg)](https://zeabur.com)

1. Fork this repository
2. Create a new project on Zeabur
3. Set environment variables in Zeabur dashboard
4. Deploy — SSL certificate and domain are handled automatically

### Option B: Self-hosted VPS

Any Ubuntu 22.04 VPS works. Recommended providers for Philippines latency:

| Provider | Region | Price | Latency to PH |
|----------|--------|-------|---------------|
| Vultr | Singapore | $6/mo | ~30ms |
| DigitalOcean | Singapore | $6/mo | ~35ms |
| Linode (Akamai) | Singapore | $5/mo | ~35ms |
| Hetzner | Helsinki | $4/mo | ~250ms |

```bash
sudo bash deploy/setup.sh   # Follow interactive prompts
```

### Updates (Zero Downtime)

```bash
sudo bash deploy/update.sh              # Pull latest + rolling restart
sudo bash deploy/update.sh --skip-pull  # Rebuild only, no git pull
```

### Backup

```bash
bash deploy/backup.sh                         # Local backup (keeps 7)
bash deploy/backup.sh --s3=s3://bucket/path   # + upload to S3/R2
```

Add to daily cron:
```bash
echo "0 2 * * * bash /opt/ph-wifi-system/deploy/backup.sh >> /var/log/ph-wifi-backup.log 2>&1" | crontab -
```

---

## Revenue Estimate

| Daily Users | Monthly Ad Revenue | Location Example |
|------------|-------------------|-----------------|
| 50 | $3 – $6 | Small café, barbershop |
| 200 | $12 – $24 | Restaurant, clinic |
| 500 | $30 – $60 | Supermarket, bus terminal |
| 1,000 | $60 – $120 | Mall food court, wet market |

💡 Add direct advertisers or paid skip (₱5/30 min) to increase revenue 5–10×.

See [docs/REVENUE-OPTIMIZATION.md](docs/REVENUE-OPTIMIZATION.md) for detailed strategies.

---

## Documentation

| Document | Description |
|----------|-------------|
| [OC200 Setup](docs/OC200-SETUP.md) | Hardware controller setup, AP adoption, Captive Portal configuration |
| [Adcash Integration](docs/ADCASH-SETUP.md) | Publisher account, ad zone creation, revenue tracking, payout |
| [Revenue Optimization](docs/REVENUE-OPTIMIZATION.md) | CPM strategies, peak hours, direct advertiser outreach, paid tiers |
| [Walled Garden](docs/WALLED-GARDEN.md) | Complete allowed-domains list for pre-auth traffic |
| [Testing](docs/TESTING.md) | End-to-end test checklist |

---

## License

MIT — Commercial use allowed with attribution.

---
---

# 繁體中文

## PH WiFi Portal — 菲律賓 WiFi 廣告營利系統

讓免費 WiFi 成為持續被動收入。用戶看 30 秒廣告，換取 1 小時免費上網。用戶零費用，你被動收益。

## 架構說明

```
OC200 硬體控制器 (192.168.0.239)
    ├── EAP225 × 2-4（室內覆蓋）
    └── EAP650-Outdoor × 1-2（戶外覆蓋，IP67 防水）

連線流程：
[用戶連接 FreeWiFi_PH]
    → OC200 重導向 HTTP 到 Portal 頁面
    → 顯示 30 秒 Adcash 廣告（Interstitial 全屏）
    → 用戶點擊「立即上網」
    → FastAPI 呼叫 OC200 API 授權 MAC 地址
    → 用戶獲得 1 小時上網
```

## 快速開始

```bash
git clone https://github.com/yourusername/ph-wifi-system.git
cd ph-wifi-system
sudo bash deploy/setup.sh
```

按照提示輸入：網域名稱、OC200 位址和帳號、Adcash Zone Key。腳本自動完成所有設定，包含 Docker 安裝、SSL 憑證申請、服務啟動、資料庫遷移。

## 硬體設備

- **TP-Link OC200** — 硬體控制器，不需要雲端，插電即用
- **TP-Link EAP225** — 室內 AP，AC1200，覆蓋約 30m
- **TP-Link EAP650-Outdoor** — 戶外 AP，AX3000，IP67 防水，覆蓋約 50m

## 部署方式

### Zeabur（推薦新手）
Fork 此 repo → 連接 Zeabur → 設定環境變數 → 部署

### 自架 VPS（完整控制）
```bash
sudo bash deploy/setup.sh
```

推薦 Vultr Singapore（$6/月，菲律賓延遲約 30ms）

## 收益估算

| 日連線人次 | 月廣告收益 |
|-----------|-----------|
| 50 人     | $3 – $6   |
| 200 人    | $12 – $24 |
| 500 人    | $30 – $60 |
| 1,000 人  | $60 – $120|

加入直接廣告主或付費跳過選項（₱5/30 分鐘），收益可提升 5–10 倍。

## 文件

- [OC200 設定指南](docs/OC200-SETUP.md) — 硬體控制器、AP 採用、Captive Portal 設定步驟
- [Adcash 整合指南](docs/ADCASH-SETUP.md) — 申請 Publisher 帳號、建立廣告 Zone、收款方式
- [收入優化指南](docs/REVENUE-OPTIMIZATION.md) — 廣告策略、高流量時段、直接廣告主接洽
